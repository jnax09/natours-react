import React from 'react'
import StyledComposition from "./composition.styles";

const Composition = ({children}) => {
    return (
        <StyledComposition>{children}</StyledComposition>
    )
}

export default Composition